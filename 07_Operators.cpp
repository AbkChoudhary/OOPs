/*Enter two numbers from keyboard. Write a program to check if the two numbers are equal.*/

#include <iostream>
using namespace std;
int main()
{
	int x,y;
	cin >> x;
	cin >> y;
	cout << (x == y) << endl;
	return 0;
}
